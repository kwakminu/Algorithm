#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n, a;
    cin >> n >> a;
    for (int i = 0; i < n; i ++) {
        int k, j;
        cin >> k >> j;
    }
    cout << "비와이";
}